csi:
  serial_port: "COM3"
  baudrate: 921600
  sample_rate_hz: 50
  auto_threshold: true

pose:
  camera_index: 0
  min_valid_keypoints: 8
  min_avg_confidence: 0.5
  mock_mode: true

api:
  # Use 0.0.0.0 when an Android phone on the same Wi-Fi should connect.
  host: "0.0.0.0"
  port: 8000
  cors_origins:
    - "*"
